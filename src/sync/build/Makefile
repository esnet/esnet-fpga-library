# ----------------------------------------------------
# Targets
# ----------------------------------------------------
all: build

build:
	vivado -mode tcl -source build.tcl

clean:
	@rm -rf out/
	@rm -f *.jou
	@rm -f *.log
