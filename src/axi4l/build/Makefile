# ----------------------------------------------------
# Targets
# ----------------------------------------------------
all: build

build:
	vivado -mode tcl -source build.tcl

clean:
	@rm -f *.jou
	@rm -f *.log
	@rm -f *.dcp
	@rm -f *.rpt
