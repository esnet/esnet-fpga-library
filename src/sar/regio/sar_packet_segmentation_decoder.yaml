name: sar_packet_segmentation
visible: true

blocks:
  counters: &counters
    !include src/packet/regio/packet_counters.yaml
  segmentation: &segmentation
    !include src/sar/regio/sar_segmentation.yaml

interfaces:
  - block: *counters
    width: 8
    address: 0x0000
    name: packets
  - block: *segmentation
    width: 14
    address: 0x4000
    name: segmentation
